# Dubai Immigration Guide

Static GitHub Pages website for `rasica-reliance.github.io`.

## Publish

In GitHub, open **Settings → Pages → Build and deployment → Source → Deploy from a branch**, then choose `main` and `/(root)`, and save.

The site uses plain HTML/CSS and does not require a build step.
